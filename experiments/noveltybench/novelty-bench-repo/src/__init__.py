# TODO(ExperimentsRefactorIssue): Keep upstream NoveltyBench sources unchanged (e.g., replace print
# statements via adapters/logging layers) instead of sprinkling style fixes directly here.
__version__ = "0.1.0"
